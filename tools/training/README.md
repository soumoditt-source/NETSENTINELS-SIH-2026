# Training commands

Training is intentionally data-dependent. Use
`tools/datasets/build_training_artifacts.py` after preparing local data. Do not
create empty model wrappers or label thresholds as trained XGBoost artifacts.
