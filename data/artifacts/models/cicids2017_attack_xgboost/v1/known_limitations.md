# Known limitations

This artifact is a supervised local XGBoost model trained only from prepared CIC-IDS2017 splits. It is a binary early-warning signal; the streaming rule and correlation layers provide the threat-specific explanations. Verify the checksums before serving it.
