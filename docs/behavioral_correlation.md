# Behavioral correlation

Legitimate service names are context, never verdicts. The correlation layer
requires independent evidence such as recurrence, destination rarity, DNS
anomaly, encrypted metadata rarity, or outbound asymmetry before raising a
composite case. It preserves individual detector provenance, related flow IDs,
and limitations. A single periodic session or a large cloud upload remains a
reviewable observation.
