# Reproducibility

Use fixed `--seed` values for the factory, safe lab, and benchmark. Keep raw
dataset files outside version control and preserve each manifest. Capture or
scenario grouping is part of the split contract. Re-run the same command on the
same input hashes before comparing metrics.
